# WhereIsThisPlace API

This directory contains the FastAPI backend for the project.

## Quick Start

```bash
poetry install
poetry run uvicorn api.main:app --reload
```

Alternatively, from the project root you can use:

```bash
bash scripts/run_api.sh
```
